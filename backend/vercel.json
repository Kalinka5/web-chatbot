{
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ],
  "ignoreCommand": "if [ \"$VERCEL_GIT_BRANCH\" = \"gh-pages\" ]; then exit 1; fi"
}
